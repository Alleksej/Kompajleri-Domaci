LET
    INT x, y, z;
    STRING str;                // Deklaracija stringa
	DOUBLE a := 1.5;
IN
    x := 10;
    y := 3;
    z := x * y;
    STRING "Hello, world!";   // Dodavanje vrijednosti stringu
    READ x;
    WRITE z * x;
    x := z * y;
    WHILE y < x
    DO
        y := y + 30;
        y := x * y;
    END;
END